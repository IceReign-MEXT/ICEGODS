{
  "name": "icegods-web",
  "version": "1.0.0",
  "scripts": {
    "start": "npx serve -s . -l 3000",
    "build": "next build",
    "install": "npm install"
  },
  "dependencies": {
    "@vercel/analytics": "^1.6.1",
    "express": "^5.2.1",
    "express-fileupload": "^1.5.2",
    "serve": "^14.2.5"
  }
}
